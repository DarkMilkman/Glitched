using UnityEngine;
using System.Collections;

public class CollectablesRestarting : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//GameObject.Find("_Globals").GetComponent<GlobalsScript>().deleteCollectablesOnLoad();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
